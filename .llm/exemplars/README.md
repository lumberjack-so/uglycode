# Exemplars

Working code patterns the agent should reuse. When building something similar to an existing exemplar, copy its structure rather than inventing a new one.

## How to use
- Before creating a new file, check this directory for a matching pattern
- If you build a clean, reusable pattern, save it here
- Name files by pattern type: `api-endpoint.py`, `react-component.tsx`, `test-pattern.py`

## Files
(none yet — the agent will populate this as it builds patterns)
