# Sprint: [Sprint Name]

## Tasks
- [ ] 1. [First task]

## Success criteria
- All tests pass (both npm test and pytest)
- No TypeScript errors (`npx tsc --noEmit`)
- No lint errors (`npm run lint`)
- Each task committed separately with conventional commit messages

## Constraints
- Use existing patterns from the codebase — do not create new abstractions
- Maximum 3 new files per task
- No new dependencies without explicit justification
